import { CustomLinkHoverDirective } from './custom-link-hover.directive';

describe('CustomLinkHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomLinkHoverDirective();
    expect(directive).toBeTruthy();
  });
});
